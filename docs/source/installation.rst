Installation
==========================================

.. toctree::
   :maxdepth: 2

.. code-block:: bash

   pip install wharf

You may need to be root or use the ``sudo`` command if you don't have access to site-packages.  Consider making
a virtual environment if this is the case.